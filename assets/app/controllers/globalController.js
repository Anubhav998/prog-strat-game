app.controller("GlobalController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';
        $log.debug("Global Controller Initialized");
    }]);